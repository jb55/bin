#! /usr/bin/env nix-shell
#! nix-shell -i bash -p mplayer
ssh monad-local "nix-shell /home/jb55/nixpkgs -p ffmpeg --run 'ffmpeg -loglevel panic -f alsa -ac 1 -i hw:2 -f ogg -'" \
  | mplayer - -idle -demuxer ogg


