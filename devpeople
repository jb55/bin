#!/usr/bin/env bash

peeps=$(cat <<EOP
Australia/Adelaide,rusty
CET,cdecker
EET,ukraine
Pacific/Fiji,fiji
EOP
)

while IFS=, read -r tz person
do
  d=$(TZ="$tz" date +'%a %F  %R')
  printf "%s\t%s\n" "$person" "$d"
done <<<"$peeps" | column -t -s $'\t'
