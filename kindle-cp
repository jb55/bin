#!/usr/bin/env bash

DEST="$HOME/Dropbox/doc/kindle/documents"
KINDLE="$(kindle-dir)"

if [ -d "$KINDLE" ]; then
    DEST="$KINDLE/documents"
fi

printf "copying to '%s'...\n" "$DEST"
for file in "$@"
do
    printf "copying '%s'\n" "$file"
done

cp "$@" "$DEST"
